# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler: resolved lockfile and locally installed gems
Gemfile.lock
.bundle/
vendor/

# Node, if the JS bundle is ever rebuilt
node_modules/

# OS cruft
.DS_Store
Thumbs.db
